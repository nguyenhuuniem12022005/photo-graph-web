# Local: leave unset — "proxy" in package.json forwards /user/* to http://localhost:5000

# CodeSandbox / production: public URL of the Express API (no trailing slash)
# REACT_APP_API_URL=https://your-backend-sandbox-5000.csb.app
